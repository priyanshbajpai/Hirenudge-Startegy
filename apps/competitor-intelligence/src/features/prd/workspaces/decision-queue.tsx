"use client";
import React from "react";
import { RecordsWorkspace } from "../records-workspace";
export function DecisionQueue(){return <RecordsWorkspace title="Founder Decision Queue" eyebrow="Separate decision from priority" subtitle="Every item keeps the recommendation, evidence, impact, dependency and founder question together. Nothing here is marked approved unless the founder changes it." defaultView="priority-board"/>}
